# 灵山胜境 AI 数字人导览 — 启动说明

## 前提条件

- Windows 10+ 电脑
- Python 3.10+（[下载](https://www.python.org/downloads/)）
- 安卓手机 + USB 数据线
- 手机开启「开发者选项」和「USB 调试」

## 1. 安装 Python 依赖

```bash
cd backend
pip install -r requirements.txt
```

## 2. 配置 LLM API Key

编辑 `backend\.env` 文件（没有则新建），写入：

```
llm_api_key=你的DeepSeek_API_Key
```

> 去 [platform.deepseek.com](https://platform.deepseek.com) 注册获取 Key

## 3. 启动后端

```bash
python run.py
```

看到 `All models preloaded — ready to serve` 即启动成功，服务运行在 `http://localhost:8000`。

## 4. 连接手机并转发端口

1. 手机 USB 连接电脑
2. 手机上允许 USB 调试授权
3. 打开终端执行：

```bash
adb reverse tcp:8000 tcp:8000
```

> 这一步让手机访问 `localhost:8000` 时转发到电脑的 8000 端口（后端）

## 5. 安装 APK

```bash
adb install app-release.apk
```

安装完成后打开手机上的「灵山AI导览」App，注册/登录即可使用。

---

## 常见问题

**Q: `pip install` 报错**
- 确认 Python 版本 ≥ 3.10：`python --version`
- 尝试 `python -m pip install --upgrade pip` 后再装

**Q: `adb` 命令找不到**
- 下载 [Android Platform Tools](https://developer.android.com/tools/releases/platform-tools)，解压后把路径加入系统环境变量 PATH

**Q: `adb reverse` 报错 "no devices"**
- 检查手机 USB 调试是否开启
- 执行 `adb devices` 确认设备列表有显示
- 换一根数据线（部分充电线不支持数据传输）

**Q: App 显示"正在连接服务器"**
- 确认后端已启动（看终端有没有 "ready to serve"）
- 确认 `adb reverse tcp:8000 tcp:8000` 执行成功
- 手机和电脑正确连接

**Q: AI 不回复**
- 确认 `backend\.env` 中 `llm_api_key` 已填写且有效
- 重启后端：`Ctrl+C` 停止后重新 `python run.py`

**Q: 数字人不说话**
- 检查手机媒体音量
- 检查后端日志是否有 TTS/LLM 错误

**Q: 地图无法显示**
- 手机需要联网（百度地图需要在线加载）
- 如果地图空白，检查手机网络连接
